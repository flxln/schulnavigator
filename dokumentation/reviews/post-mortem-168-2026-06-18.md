# Post-Mortem #168 — DELETE Hotspot Fehler-Mapping

**Datum:** 2026-06-18
**Plan:** [mpz_delete_hotspot_#168_0465be41.plan.md](../../.cursor/plans/mpz_delete_hotspot_%23168_0465be41.plan.md)
**Issue:** [#168](https://github.com/flxln/schulnavigator/issues/168)

---

## 1. Todos

| id | Befund |
|----|--------|
| `delete-map-hotspot-error` | ✅ |
| `delete-route-test-400` | ✅ |
| `sync-comment-client-error-codes` | ✅ |
| `verify-test-build` | ✅ (laut Plan-Frontmatter als completed markiert) |
| `docs-delete-mapping` | ✅ |

Alle fünf Todos im Frontmatter sind als `completed` markiert und decken sich mit dem vorgefundenen Code.

---

## 2. Änderungslog-Einträge

¹ **`await import` im Test-Snippet** | ✅ | `route.test.ts` Z. 119 nutzt `const { MpzStationHotspotsError } = await import('@/lib/mpz-station-hotspots')` — identisch mit dem Plan-Snippet; kein statischer Import der Klasse.

² **Sync-Kommentar `CLIENT_ERROR_CODES` + Follow-up** | ✅ | `[hotspotId]/route.ts` Z. 13: `// sync with hotspots/route.ts; Single Source of Truth folgt im Dedup-Follow-up`. `hotspots/route.ts` Z. 12: `// sync with [hotspotId]/route.ts; Single Source of Truth folgt im Dedup-Follow-up`. Beide Kommentare präzise wie im Plan.

³ **NOT_EDITABLE-Hinweis am POST-Ternary** | ✅ | `hotspots/route.ts` Z. 40–41: Kommentar direkt über dem Inline-Ternary; Wortlaut entspricht dem Plan (`NOT_EDITABLE bewusst nicht gemappt — addStationHotspot wirft ihn nicht; bei Domain-Erweiterung mapHotspotError aus [hotspotId]/route.ts nutzen`). Keine Logikänderung.

⁴ **Doku-Aktualisierung in Scope** | ✅ | `fuer-entwickler.md` Z. 50 beschreibt das Fehler-Mapping vollständig inkl. `NOT_EDITABLE nur bei PATCH → 403 (#168)` und verweist nicht mehr auf eine offene Inkonsistenz. `epic-mpz-studio-v1.md` Z. 4 und Z. 19: `#168` als erledigt gelistet (`✓`). `technische-fragen.md`: kein offener Punkt zu #168 vorhanden.

⁵ **Offene Fragen verbindlich beantwortet** | ✅ | Plan enthält unter „Offene Fragen" keine ungelösten Punkte mehr; beide Entscheidungen (kein POST-Refactor im PR, Doku in Scope) sind im Code und in der Doku nachgezogen.

---

## 3. Entscheidungstabelle

| Entscheidung | Code | Befund |
|---|---|---|
| DELETE nutzt `mapHotspotError` in derselben Datei | `[hotspotId]/route.ts` Z. 138–141: `if (err instanceof MpzStationHotspotsError) { const mapped = mapHotspotError(err); return NextResponse.json(mapped.body, { status: mapped.status }) }` | ✅ Identisch mit Plan |
| `NOT_EDITABLE` → 403 defensiv toleriert | `mapHotspotError` Z. 82–84 enthält den Zweig; DELETE ruft ihn jetzt auf | ✅ |
| Keine Domain-Änderung | `removeStationHotspot` unverändert | ✅ |
| Test-Klasse per `await import` | `route.test.ts` Z. 119 | ✅ |
| POST-Refactor → Follow-up | Kein Extrakt, nur Sync-Kommentare | ✅ |
| Sync-Kommentare + NOT_EDITABLE-Hinweis | Beide Route-Dateien aktualisiert | ✅ |
| Doku in Scope | `fuer-entwickler.md`, `epic-mpz-studio-v1.md` aktualisiert | ✅ (siehe Abweichung unten) |

**Fehler-Mapping DELETE (nach Fix) — Vergleich Plan vs. Code:**

| `err.code` | Plan (HTTP) | Code (`mapHotspotError`) | Befund |
|---|---|---|---|
| `NOT_FOUND` | 404 | Z. 79–81 | ✅ |
| `NOT_EDITABLE` | 403 | Z. 82–84 | ✅ |
| `CLIENT_ERROR_CODES` | 400 | Z. 85 | ✅ |
| Sonst | 500 | Z. 85 (Fallback) | ✅ |

`MpzContentIoError`-Handling in DELETE: `VALIDATION` → 422, sonst → 500 — unverändert (Z. 142–145). ✅

---

## 4. Scope

**Im Code, aber nicht explizit im Plan:**

- `route.test.ts` Z. 116–126: Der neue DELETE-Test `'INVALID_COORDS → 400'` ist vollständig vorhanden — kein struktureller Unterschied zum Plan-Snippet. Der `afterEach` (Z. 64–68) ruft `vi.restoreAllMocks()` zusätzlich zu `vi.unstubAllEnvs()` und `mockReset()` auf. Der Plan erwähnt nur `mockReset()` als vorhandene Teardown-Logik; `restoreAllMocks` ist ein stiller Zusatz ohne Risiko (der Test-Scope ist korrekt isoliert).

- Die Zeilennummern der `MpzContentIoError`-Behandlung in DELETE (Z. 142–145 vs. Plan Z. 142–145) stimmen exakt überein — kein stiller Shift.

Keine tangentialen Änderungen außerhalb der im Plan gelisteten Dateien.

---

## Gesamtbewertung

Plan und Code stimmen vollständig überein. Alle Pre-Mortem-Fixes aus dem Änderungslog sind umgesetzt, alle Todos erledigt, alle Entscheidungen korrekt nachgezogen. Kein Korrekturbedarf.

**Eine kleinere Beobachtung ohne Handlungsbedarf:**

### Epic-Index: #168-Post-Mortem-Link fehlt — Plan sagt nichts, Code macht nichts

- **Risiko:** Harmlos. Der Plan spezifiziert keinen Post-Mortem-Link im Epic-Index. `epic-mpz-studio-v1.md` Z. 93–94 verlinkt die Post-Mortems zu #167 und #163, nicht zu #168 — dies ist eine Konventionslücke, keine Planabweichung.
- **Empfehlung:** Plan nachziehen — Post-Mortem-Link zu #168 in `epic-mpz-studio-v1.md` ergänzen (analog Z. 93–94), oder als Teil des Doku-Abschluss-Issues #164 aufnehmen.
