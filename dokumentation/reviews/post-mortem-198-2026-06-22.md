---
tags:
  - post-mortem
  - mpz-studio
  - issue-198
erstellt: 2026-06-22
---

# Post-Mortem 4 — Plan-Code-Abgleich Issue #198

## 1. Todos
| id | Befund |
|---|---|
| redirects | ✅ |
| station-tabs | ✅ |
| medien-only | ✅ |
| dialog-panel-link | ✅ |
| cleanup-delete | ✅ |
| tests-doku | ✅ |

Alle Todos wurden präzise nach Plan umgesetzt.

## 2. Änderungslog-Einträge (inkl. Pre-Mortem Fixes)
| Fix-Titel | Befund | Kommentar |
|---|---|---|
| ¹ 1b #1+#2 Mechanismus für `/ingest` auf page-only | ✅ | Korrekt in `ingest/page.tsx` als UI-lose Server-Component mit `redirect()` umgesetzt. |
| ³ 1b #1 `next.config.ts` ohne `/ingest` Redirect | ✅ | Die Config enthält nur den permanenten Redirect für `dialog-audio`. |
| ⁴ 1b #1 `next.config.test.ts` ohne ingest-Test | ✅ | Test für `ingest` wurde entfernt. |
| ⁵ 1b #1 `ingest/page.tsx` UI-lose Redirect-Stelle | ✅ | Setzt `await searchParams` und `encodeURIComponent` ein. |
| ⁶ 1a #1 `[slug]/page.tsx` `searchParams` await | ✅ | `searchParams` wird als Promise aufgelöst und nach `isHubSlug`-Check validiert. |
| ⁷ 1a #3/1b #3 `resolveTab` Sonderregel | ✅ | In `station-detail-shell.tsx` wird `dialog-audio` vor `VALID_TABS` gematcht. |
| ⁸ 1a #2 URL-Encoding Grid/Hotspot | ✅ | Link-URLs in `station-grid.tsx` und `station-hotspot-add-form.tsx` nutzen `encodeURIComponent(slug)`. |
| ⁹ 1b #4 `station-dialog-panel` Ersatztext | ✅ | Statischer Hinweis für CLI/curl und #200 ist eingebaut. |
| ¹² 1b #3 Testabdeckung Redirect/Fallback | ✅ | Fallback-Logik in `station-detail-shell.test.tsx` integriert. |

Alle relevanten Pre-Mortem-Änderungen und Fixes wurden vollständig im Code reflektiert.

## 3. Entscheidungstabelle
Der Code entspricht allen getroffenen Entscheidungen. Der Redirect für `/ingest` wurde wie verlangt nur in `page.tsx` realisiert (Option A), wodurch Query-Parameter korrekt ausgewertet werden. Der Fallback in `resolveTab` wurde präzise als erste Sonderregel eingefügt.

## 4. Scope
Der Code weicht nicht vom Plan ab. Es gibt keine tangentialen Änderungen oder stillschweigenden Refactorings. Die gelöschten Dateien (`mpz-studio-ingest-opener.tsx` und `dialog-audio-panel.tsx`) sind erfolgreich entfernt worden.

Fazit: Plan und Implementierung stimmen vollständig überein.
