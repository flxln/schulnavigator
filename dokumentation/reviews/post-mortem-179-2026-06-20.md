# Post-Mortem 4 — Plan-Code-Abgleich nach Implementierung (Issue #179)

**Datum:** 2026-06-20  
**Feature:** Hub-Config Studio (#179)  
**Grundlage:** [hub-config_studio_#179_7fb73f5e.plan.md](../../.cursor/plans/hub-config_studio_#179_7fb73f5e.plan.md)

## 1. Todos
Alle Todos aus dem Frontmatter des gehärteten Plans wurden überprüft.

| id | Befund |
|---|---|
| config-loaders | ✅ Vollständig umgesetzt. Die 3 JSON-Dateien, Validatoren (`mpz-hub-config-validation.ts`), Fallbacks (`FALLBACK_HUB_SLUG_MAP`) und das Build-Skript `validate-hub-config.mjs` (registriert in `package.json`) sind vorhanden. |
| consumers-refactor | ✅ Vollständig umgesetzt. `validateStationsFile` und `buildHubStations` nutzen nun `hubSlugMap` explizit. |
| io-domain-api | ✅ Vollständig umgesetzt. `MpzContentIo` Interface erweitert, IO-Methoden implementiert, `replaceHubConfig` Domain-Funktion mit `MpzHubConfigError` existiert. |
| studio-ui | ✅ Vollständig umgesetzt. Die Studio UI Komponenten wurden entsprechend umgesetzt. |
| verify-docs | ✅ Vollständig umgesetzt. Test und Build Scripte sind konfiguriert. |

## 2. Änderungslog-Einträge
Das Änderungslog aus dem gehärteten Plan (Pre-Mortem-Fixes) wurde verifiziert:

| Fix | Befund |
|---|---|
| ¹ expliziter `hubSlugMap`-Parameter für `validateStation` | ✅ `validateStation` erhält den Parameter explizit und `EXPECTED_STATION_COUNT` wird als lokale Variable innerhalb von `validateStationsFile` berechnet. |
| ² Drei-Datei-Rollback-Protokoll explizit spezifiziert | ✅ `commitHubConfigWrite` in `mpz-content-io.ts` implementiert das geforderte Pattern (tmp schreiben, Inline/Cross-Validierung, rename, Error-Handling mit tmps löschen und `.bak` restore). |
| ³ `buildHubStations` bekommt kein `accents`-Parameter | ✅ `BuildHubStationsOptions` erlaubt nur `hubSlugMap`. Akzente werden im Validator eigens geprüft. |
| ⁴ `HubSlug` als explizite String-Literal-Union | ✅ In `schoolhouse-hub-map.ts` explizit als Union definiert, bevor die Map `FALLBACK_HUB_SLUG_MAP` mit Annotation darauf aufbaut. |
| ⁵ `parsePutBody` prüft nur Struktur (`INVALID_BODY`) | ✅ `parsePutBody` prüft exklusiv die Struktur, fachliche Semantik wirft Post-Write den `VALIDATION` Fehlercode. |
| ⁶ `type: "image"` gestrichen | ✅ `validateIcons` (mjs) und `isIconRecord` (ts) erzwingen `{ type: "lucide", name: string }`. |

## 3. Entscheidungstabelle
Alle wesentlichen Entscheidungen wurden präzise eingehalten:
- **Fehler-Mapping:** Entspricht der Entscheidung. Das UI wird strukturell via 400 `INVALID_BODY` (Domain) abgewiesen, semantisch via 422 `VALIDATION` (IO/Post-Write).
- **Typ-Signaturen:** `HubSlug` ist eine explizite Union, Alias exportiert keinen Typ.
- **Merge-Reihenfolge:** Der Drei-Datei-IO-Lock mit tmp-Dateien, Validierung und ggf. Rollback (mit `.bak`) ist exakt wie spezifiziert umgesetzt.

## 4. Scope
Der Code entspricht dem Plan sehr präzise. Es gibt keine versteckten Refactors oder Abweichungen vom dokumentierten Umfang. Es wurden keine unautorisierten Erweiterungen im Code festgestellt.
