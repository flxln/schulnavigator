---
tags:
  - verifikation
  - 04-post-mortem
erstellt: 2026-06-23
---
# Post-Mortem 4 — Plan-Code-Abgleich nach Implementierung

**Issue:** #200 (Dialog-Editor)
**Datum:** 2026-06-23
**Plan:** `dialog-editor_#200_ec35f5cf.plan.md`

## 1. Todos

| id | Befund |
|---|---|
| domain-clip-delete | ✅ Erledigt. `removeDialogClip` löscht nur die Datei, kein JSON-Write. |
| api-clip-delete | ✅ Erledigt. DELETE Route implementiert, `VALIDATION` Error mit `#199`-FIXME. |
| audio-row-component | ✅ Erledigt. `station-dialog-segment-audio-row.tsx` erstellt, mit Upload, Clip-Delete, Inline-URL und `onError`-Hinweis. |
| panel-integration | ✅ Erledigt. Sub-Zeilen integriert, Collapsibles als `div`/Flex (keine verschachtelten Buttons), `#200`-Banner weg, `validateNow` eingebunden. |
| badge-labels | ✅ Erledigt. Labels in `dialog-audio-status-badges.tsx` auf "Audio fehlt" aktualisiert. |
| tests-doku | ✅ Erledigt. Spec 15 aktualisiert, Komponenten und Routen getestet/dokumentiert. |

## 2. Änderungslog-Einträge (Pre-Mortem Fixes)

| ID | Befund | Bemerkung |
|---|---|---|
| ¹ 1a #1 (Play-URL inline) | ✅ | URL wird clientseitig gebaut (`/api/dialog/${slug}/${expectedClip}`), Server-Modul-Import vermieden. |
| ² 1a #2 (Collapsible-Header) | ✅ | `<button>` und „Gruppe hinzufügen“ sind korrekte Siblings in einem Flex-Container. |
| ³ 1a #3 (`validateNow()`) | ✅ | `validateNow()` wird nach Upload und Clip-Delete aufgerufen, um das Top-Bar-Badge zu aktualisieren. |
| ⁴ 1b #1 (Spec 15) | ✅ | Spec 15 (Z. 120) wurde korrekt um `DELETE …/clip` ergänzt. |
| ⁵ 1b #2 (`VALIDATION` + FIXME) | ✅ | Modulkonsistenz gewahrt, FIXME für `#199`-Tech-Debt ist in Domain und Route vorhanden. |
| ⁶ 1b #3 (`onError`-Hinweis) | ✅ | `<audio>` hat `onError`-Handler mit Hinweis auf `/eintritt` / `SN_DEV_UNLOCK_ALL`. |
| ⁷ Offene Frage 1 (Warnbanner) | ✅ | Schlankes Warnbanner für Orphans/Drift/Missing bleibt erhalten, `#200`-Text wurde entfernt. |
| ⁸ Offene Frage 2 (Single-Open) | ✅ | State `expandedAudioSegmentId` sorgt dafür, dass maximal eine Audio-Zeile offen ist. |

## 3. Entscheidungstabelle

Der Code entspricht allen getroffenen Entscheidungen.
- **Expandable Row statt Popover:** Umgesetzt via `<tr>` mit `colSpan={7}`.
- **Play-URL inline:** Korrekt umgesetzt in `StationDialogSegmentAudioRow`.
- **Clip-DELETE ohne JSON-Write:** `removeDialogClip` führt nur `unlink` aus, kein Neuschreiben des JSONs.
- **Gruppen/Bubble default zugeklappt:** States `gruppenOpen` und `bubbleOpen` sind initial `false`.
- **Figuren nicht einklappbar:** Figuren-Checkboxes bleiben separat und immer sichtbar.

## 4. Scope

Der Code hält sich strikt an den Plan. Es wurden keine tangentialen Änderungen, unerwarteten Erweiterungen oder stillen Refactors durchgeführt.

---
Plan und Code stimmen vollständig überein. Die Implementierung ist valide und kann abgeschlossen werden.
