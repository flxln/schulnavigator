---
tags:
  - post-mortem
  - issue-201
  - mpz-studio
  - kalibrierung
erstellt: 2026-06-23
---

# Post-Mortem 4 — Plan-Code-Abgleich nach Implementierung

**Issue:** #201 (Sphere-Kalibrierung)
**Datum:** 2026-06-23
**Plan:** `.cursor/plans/sphere-kalibrierung_#201_9fa39b03.plan.md`

## 1. Todos

| ID | Befund |
|---|---|
| `viewer-calib-props` | ✅ |
| `sphere-calib-components` | ✅ |
| `sphere-calib-shell-route` | ✅ |
| `calib-href-links` | ✅ |
| `tests-doku-build` | ✅ |

**Befund:** Alle Todos wurden strikt und vollständig gemäß den Planvorgaben umgesetzt.

## 2. Änderungslog-Einträge (Pre-Mortem Fixes)

| ID | Befund | Kommentar |
|---|---|---|
| ¹ 1a #1 (`calibEnabled` Folding) | ✅ | Exakt wie vorgegeben in `sphere-raum-viewer-inner.tsx` implementiert: `calibMode` wird in `calibEnabled` gefaltet. |
| ² 1a #2 (Refs für Callbacks) | ✅ | Die Callbacks laufen sauber über `onCalibClickRef` und `onCalibViewReadyRef` – stale Closures werden dadurch verhindert. |
| ³ 1a #3 (Hoisting des PSV) | ✅ | Der Viewer wird in der Shell nur einmal gemountet. Der Tab-Wechsel tauscht lediglich die rechten UI-Panels aus. |
| ⁴ 1a #4 (`hasPanorama360`) | ✅ | Der Parameter wurde durchgängig in allen Hilfsfunktionen, der Overview-Berechnung, den Link-Callsites und den Tests ergänzt. |
| ⁵ 1b #1/#4 (Guard ohne Message) | ✅ | Der Studio-Guard wurde richtlinienkonform nicht um eine `message` erweitert. |
| ⁶ 1b #3 (`VALIDATION` beibehalten) | ✅ | Status Quo beibehalten, keine Rename-Versuche. |
| ⁷ 1b #6 (`IO` als gültiger Code) | ✅ | Status Quo beibehalten. |
| ⁸ Offene Frage 1 (Shared Persist) | ✅ | Die POST-Logik wurde wie angewiesen nach `mpz-sphere-calib-persist.ts` ausgelagert. |
| ⁹ Offene Frage 2 (Marker Rendering) | ✅ | Das Runtime-Rendering des Viewers wurde wiederverwendet; `hotspots360[]` wird an den PSV in der Shell übergeben. |
| ¹⁰ Offene Frage 3 (Kein Copy JSON) | ✅ | Die Panels auf der neuen S14 Route verzichten auf einen „JSON kopieren“-Button; das Legacy-Overlay behält die Funktion. |

**Befund:** Die gehärteten Fixes aus dem Pre-Mortem wurden 1:1 in den Code übernommen und fehlerfrei integriert.

## 3. Entscheidungstabelle

Die architektonischen Entscheidungen des Plans spiegeln sich im Code vollständig wider. Insbesondere die Vermeidung von Viewer-Remounts durch das Hoisting des WebGL-Kontextes sowie die klare Trennung von Overlay und dedizierter Studio-UI (`calibUi="external"`) wurden präzise realisiert. Der Scope blieb fokussiert, es wurden keine neuen API-Routen eingeführt.

## 4. Scope

- **Zusätzliche Komponente (`sphere-startblick-crosshair.tsx`):** Der Plan erwähnte ein Fadenkreuz für den Startblick-Tab. Im Code wurde dieses Fadenkreuz in eine eigene Datei ausgelagert. Dies ist eine sinnvolle Kapselung für die saubere Trennung der UI-Belange und entspricht der im Plan geforderten Funktionalität vollumfänglich.

---

**Fazit:** 
Plan und Code stimmen vollständig überein. Die Implementierung der Sphere-Kalibrierung ist präzise, performant und weicht nicht von den etablierten Projekt-Vorgaben ab. Keine Korrekturen erforderlich.
