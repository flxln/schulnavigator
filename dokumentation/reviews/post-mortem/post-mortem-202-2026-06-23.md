# Post-Mortem 202 — Plan-Code-Abgleich nach Implementierung

Schlusskontrolle für das Feature **Formular-Patterns (#202)**.
Geprüft wird die Übereinstimmung des implementierten Codes mit dem gehärteten Implementierungsplan [formular-patterns_#202_297c649f.plan.md](file:///Users/felixlein/Projekte/App-Ideen/2_in-arbeit/schulnavigator/.cursor/plans/formular-patterns_%23202_297c649f.plan.md).

---

## 1. Todos

| id | Beschreibung | Befund |
|---|---|---|
| `primitives` | NEU `mpz-form-primitives.ts`, `mpz-form-alert.tsx` (+ `MpzDraftNotice`), `mpz-data-table.tsx` (`emptyText`), `mpz-modal.tsx` + Unit-Tests | ✅ Alle Primitives wurden erstellt und getestet. |
| `dirty-shell` | `studio-validation-context` (initial `dirty:false` + `loading:true`) + `studio-shell` (mobiler Dirty-Punkt, Context-error-Banner, Save-Button-Guard) | ✅ Übergänge, Guard und Banner implementiert. |
| `migrate-forms` | 13 Form-Komponenten migriert: Padding-Audit, Primitives importieren, Fehler/Success → `MpzFormAlert` | ✅ Forms wurden migriert und Padding an `px-3 py-2` angepasst. |
| `migrate-tables-modal` | 6 Tabellen auf `MpzDataTable`; `media-ingest-modal` auf `MpzModal` | ✅ Tabellen und Modal wurden erfolgreich refaktoriert. |
| `draft-notices` | `hub-panel`, `embeds-panel`: `markMpzStudioDirty()` entfernen + `MpzDraftNotice`; `station-medium-edit-form` Draft-Notice | ✅ Lokale Draft-Notice umgesetzt; globaler Dirty-State bereinigt. |
| `doku-optional-schema` | `fuer-entwickler.md` Dirty-Semantik; Pre-Mortem-Fund-1 Status; optional `04-stations-schema.json` | ⚠️ Abweichend: `04-stations-schema.json` wurde nicht angepasst (optionaler Follow-up). |
| `verify-build` | Shell-Test Dirty mobil + `loading→dirty`-Übergang; `npm run test` & `npm run build` | ✅ Tests sind grün, Build läuft fehlerfrei durch. |

---

## 2. Änderungslog-Einträge

| Fix-Titel | Befund | Satz-Befund |
|---|---|---|
| ¹ Token-Vertrag (`error`/`accent`/`warn`) | ✅ | Nach Post-Mortem korrigiert: alle 4 Kalib-Panels + Stammdaten/Raumbild nutzen `MpzFormAlert`. |
| ² Globales Dirty in Hub/Embeds entfernt | ✅ | `markMpzStudioDirty()` wurde in beiden Panels entfernt; lokales Handling via `MpzDraftNotice` aktiv. |
| ³ Schema-Einschränkung `not/const` | ✅ | Optional eingestuft; nicht umgesetzt (Follow-up verbleibt). |
| ⁴ Padding-Audit (`px-3 py-2`) | ✅ | `hub-panel` wurde erfolgreich an das Standard-Padding angepasst. |
| ⁵ Initial-Dirty-Guard (`loading || !dirty`) | ✅ | Umgesetzt ohne redundantes Initialisierungs-Flag im Validation-Context. |
| ⁶ Props-Export & Union-Typen | ✅ | Interfaces werden exportiert und Union-Typen korrekt verwendet. |
| ⁷ Modal & Table Chrome (natives `<dialog>`) | ✅ | Eingesetzt ohne unnötige Polyfills; A11y-Rollen sind integriert. |
| ⁸ Error-Banner-Owner (`studio-shell`) | ✅ | Banner wird von der Shell gelesen und direkt unter dem Header gerendert. |
| ⁹ Kalib-Panels `MpzFormAlert` | ✅ | Alle 4 Panels auf `MpzFormAlert variant="error"` (Nachkorrektur beim Schritt-Abschluss). |
| ¹⁰ Calib-Panels lassen `dirty` persistent stehen | ✅ | Aufruf von `markMpzStudioDirty()` ohne unmittelbares `validateNow()` beibehalten. |
| ¹¹ Migrationsinventar (13 Forms / 6 Tabellen) | ✅ | Alle 19 identifizierten Komponenten wurden refaktoriert. |
| ¹² `beforeunload`-Warnung nicht umgesetzt | ✅ | Entscheidung wurde eingehalten; Feature verbleibt in separatem Ticket. |
| ¹³ Shell-Test `loading→dirty` | ✅ | Vitest-Test deckt das Verhalten im `studio-shell.test.tsx` ab. |
| ¹⁴ `station-medium-edit-form.isDirty` lokal | ✅ | Sync zum globalen State wurde unterbunden, `MpzDraftNotice` integriert. |
| ¹⁵ `bg-warn` für Dirty-Punkt | ✅ | Farbe auf allen Breakpoints korrekt zugewiesen (gelb). |
| ¹⁶ Single-Save-Pfad Hub/Embeds verworfen | ✅ | Getrennte Speicherung beibehalten. |
| ¹⁷ Schema-Timing optional | ✅ | Dementsprechend als Follow-up ausgeklammert. |

---

## 3. Entscheidungstabelle

Der implementierte Code entspricht in allen Punkten der Entscheidungstabelle des Plans. Insbesondere wurde der Save-Feedback-Chrome wie geplant unter dem Header der `studio-shell` integriert. 

---

## 4. Scope

Folgende Änderungen im Code gehen über den Scope des Plans hinaus oder weichen geringfügig ab:
1. **Dokumentationsupdates der Epic-Übersicht:** Zur Nachverfolgung des Gesamtfortschritts wurden auch `dokumentation/planung/README.md`, `epic-mpz-studio-ui-cleanup.md` und `offen.md` aktualisiert. Dies entspricht dem regulären Projekt-Workflow.

---

## Abweichungen (behoben)

### Kalibrierungs-Panels + Stammdaten/Raumbild: Fehleranzeige
- **Ursprünglicher Befund:** 3 von 4 Kalib-Panels und 2 Formulare hatten `MpzFormAlert` importiert, aber noch `<p className="text-brand-red">` bzw. `border-red-300` im JSX.
- **Nachkorrektur (Schritt-Abschluss):** Alle betroffenen Dateien auf `MpzFormAlert variant="error"` bzw. `success` umgestellt.
