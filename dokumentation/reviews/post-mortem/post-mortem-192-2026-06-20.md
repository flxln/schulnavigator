---
title: "Post-Mortem: Coach Layout #192"
date: "2026-06-20"
---

# Post-Mortem 4 — Plan-Code-Abgleich nach Implementierung

Schlusskontrolle: Hat die Implementierung den gehärteten Plan tatsächlich umgesetzt? Kein allgemeines Code-Review — nur der Abgleich Plan → Code.

Vorgelagert: [[02-plan-haerten]] (der Plan, gegen den geprüft wird)
Verwandt: [[code-review-defensiv]] (für allgemeines Review)

---

**1. Todos**

| id | Befund |
|---|---|
| types-coach-layout | ✅ |
| validator-domain | ✅ |
| api-patch-layout | ✅ |
| overlay-styles | ✅ |
| verify-build | ✅ |

Die Todo-Liste wurde vollständig und planungskonform abgearbeitet. Die TypeScript-Typen, Validatoren und das Frontend-Mapping sind akkurat umgesetzt.

**2. Änderungslog-Einträge**

| Fix | Befund | Beschreibung |
|---|---|---|
| ¹ 1b#1 `bubbleMaxWidth` | ✅ | `bubbleMaxWidth` ist korrekt als absoluter rem-Wert implementiert (`min(100%, 22rem)`). Clamps sind in TS und im MJS-Validator identisch. |
| ² 1b#2 `mascotSize` | ✅ | `resolveCoachLayout` nutzt eigene Konstanten und Clamps, explizit unabhängig von ADR-014. |
| ³ 1b#3 `bubbleOffset*` | ✅ | Das Delta zur CSS-Default-Position wird im Resolver exakt über `baseBubbleMarginY + bubbleOffsetY` abgebildet. Der Studio-Hinweis ist vorhanden. |
| ⁴ 1a#1 Leere Felder strippen | ✅ | `CoachMessageForm` (in `buildLayoutPayload`) überspringt leere Strings und NaN-Eingaben beim Formular-Submit sicher. |
| ⁵ 1a#2 `MascotFigure` Props | ✅ | `style` und `imgStyle` (als `React.CSSProperties`) wurden in die Komponentensignatur eingefügt und im DOM verankert. |
| ⁶ 1a#3 Offset-Strategie | ✅ | `marginTop` für Blasen-Y, `marginLeft` für Blasen-X und `transform` für Mascot-Versatz sind als Property-Mapping angewendet worden. |
| ⁷ Kein Pilot-`layout` | ✅ | `coach-messages.json` blieb unangetastet. |
| ⁸ Reset-Button | ✅ | Der Button "Auf Standard" schickt erwartungskonform `layout: null` im PATCH an die API, was das Feld persistiert bereinigt. |

Alle Pre-Mortem-Fixes aus dem Änderungslog wurden detailgetreu im Code abgebildet.

**3. Entscheidungstabelle**
Alle wesentlichen Architektur- und Designentscheidungen aus dem Plan sind im Code nachvollziehbar:
- **Inline-Styles statt CSS-Klassen**: Realisiert via `CSSProperties` im Resolver und direkter DOM-Zuweisung.
- **TypeScript-Defaults**: Ausgelagert in `app/lib/coach-layout.ts`.
- **Kein Deep-Merge beim PATCH**: Im `patchCoachMessage`-Backend direkt umgesetzt (`patch.layout` überschreibt `merged.layout`).
- **Domain-Fehler-Mapping**: Erweitert `COACH_CLIENT_ERROR_CODES` passend um `INVALID_LAYOUT` und mappt als HTTP 422.

**4. Scope**
Der Code bewegt sich strikt innerhalb der Grenzen des Implementierungsplans. Es sind keine tangentialen Veränderungen, undokumentierten Refactorings oder zusätzlichen Scope-Erweiterungen (Feature Creep) hinzugekommen. Das Modul ist sauber vom Rest der App isoliert umgesetzt worden.

**Nachtrag (gleicher Branch):** `mascotFlipX` / `mascotFlipY` ergänzt (horizontal/vertikal spiegeln) — kleine Scope-Erweiterung nach Plan-Abschluss, analog Dialog `mascotFlipX` (ADR-014).
