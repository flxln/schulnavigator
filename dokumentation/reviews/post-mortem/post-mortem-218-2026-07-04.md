---
tags:
  - post-mortem
  - issue-218
  - design-hub
erstellt: 2026-07-04
---

# Post-Mortem #218 — D1 Design & Hub (S19, S20)

**Abgleich zwischen Issue [#218](https://github.com/flxln/schulnavigator/issues/218) und Implementierung auf Branch `feature/mpz-studio`.**

Visuelles Polish für Design-Seite, Hub-Tabelle und Brand-Panel — ausschließlich Primitives und Layout, keine API- oder IA-Änderungen. Hub bleibt Tabelle (Epic-Entscheidung).

## Umsetzung

| Bereich | Befund |
|---------|--------|
| `design-page-shell.tsx` | ✅ `mpzTabLinkClassName` (Underline-Tabs), `mpzStackClassName('lg')`, `font-semibold` für H1 |
| `design/page.tsx` | ✅ Brand- und Hub-Inhalt in `MpzCard` statt manueller Section-Klassen |
| `brand-panel.tsx` | ✅ `mpzButtonClassName('secondary')` für Upload, `text-error`, Accent-Links, `font-semibold` |
| `hub-panel.tsx` | ✅ Tabelle beibehalten; `mpzStackClassName`, `mpzButtonClassName('primary')`, Tabellen-Border/Rand, `px-3` |

## Akzeptanzkriterien (Issue)

- [x] Tabs optisch an Station-Detail angeglichen (`mpzTabLinkClassName`)
- [x] Hub-Tab: **Tabelle** — poliert, kein Karten-Grid
- [x] Brand-Panel auf Primitives migriert
- [x] `npm test` grün (145 Dateien, 1110 Tests)

## Bewusst offen

- Brand-Slot-Karten innerhalb des Panels ohne eigene `MpzCard`-Unterteilung (Mockup S20 zeigt Karten-Grid nur für Slots, nicht für Hub).
- Hub-Tabelle: kein Empty-State nötig (fix 12 Stationen).

## Verwandte Dokumente

- [epic-mpz-studio-v3-visual-polish.md](../../planung/epic-mpz-studio-v3-visual-polish.md)
- Parent Epic [#205](https://github.com/flxln/schulnavigator/issues/205)
