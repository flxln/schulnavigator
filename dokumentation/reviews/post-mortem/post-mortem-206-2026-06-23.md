# Post-Mortem 206 — Plan-Code-Abgleich nach Implementierung

Schlusskontrolle für das Feature **Studio Tokens (#206)**.
Geprüft wird die Übereinstimmung des implementierten Codes mit dem gehärteten Implementierungsplan [#206_studio_tokens_6e344e61.plan.md](file:///Users/felixlein/Projekte/App-Ideen/2_in-arbeit/schulnavigator/.cursor/plans/%23206_studio_tokens_6e344e61.plan.md).

---

## 1. Todos

| id | Beschreibung | Befund |
|---|---|---|
| `tokens-css` | `NEU: app/app/mpz-studio-tokens.css` — :root-Aliase auf GS39 (sidebar 240px, topbar 56px, card 8px/20px, stripe 4px); KEIN `--mpz-focus-ring` | ✅ **Erledigt**: Die Datei `app/app/mpz-studio-tokens.css` wurde exakt wie geplant angelegt. Alle Layout-Maße sind als Aliase auf GS39-Variablen (`var(--gutter)`, `var(--s-2)`, `var(--r-pill)`, `var(--s-1)`) oder als Literals definiert. Es wurden keine neuen Hex-Farben und kein `--mpz-focus-ring`-Token verwendet. |
| `globals-theme` | `MODIFY: globals.css` — Import + `@theme inline` für ALLE 6 mappbaren Tokens (`--spacing-*/--radius-*`); Stripe-Breite NICHT als Theme-Key (border-l-4 built-in) | ✅ **Erledigt**: `@import './mpz-studio-tokens.css';` wurde eingebunden. Alle 6 neuen dimensionalen/Radius-Keys wurden in `@theme inline` registriert. Die Stripe-Breite wurde wie geplant nicht als Theme-Key registriert (da built-in `border-l-4` genutzt wird). |
| `primitives-extend` | `MODIFY: mpz-form-primitives.ts` — Field-Focus (outline-none+ring), Button-/Sidebar-Helfer, `mpzStackClassName` MIT `flex flex-col`; Signaturen stabil, Field-Rückgabe = Superset | ✅ **Erledigt**: Alle geforderten Helfer wurden implementiert. `mpzFieldClassName` wurde als Superset (inkl. `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-alt`) erweitert, um Rückwärtskompatibilität zu wahren. `mpzStackClassName` bringt das Layout-Modell `flex flex-col` mit. Die 3 bestehenden Signaturen blieben stabil. |
| `mpz-card` | `NEU: mpz-card.tsx` — default/validation (validation = `border-l-4 border-l-accent`, KEIN Tint, KEIN Shadow); + `mpz-card.test.tsx` | ✅ **Erledigt**: `MpzCard` wurde mit den Varianten `default` und `validation` angelegt. Die `validation`-Variante verwendet einen grünen Akzentstreifen links (`border-l-4 border-l-accent`), verzichtet auf Shadow/Tint und wurde in `mpz-card.test.tsx` gründlich getestet. |
| `primitives-tests` | `NEU: mpz-form-primitives.test.tsx` für Klassenverträge (inkl. Field-Superset + Stack-Layout) | ✅ **Erledigt**: Die Testdatei existiert und verifiziert alle Primitives-Klassenverträge (einschließlich Superset-Prüfung für Fields und Stack-Layout). |
| `verify` | validate:tokens + npm test + npm run build grün | ✅ **Erledigt**: `validate:tokens` läuft erfolgreich durch. Alle 1056 Unit-Tests sind grün. Die Next.js-Produktionskompilation (`npx next build`) läuft fehlerfrei durch. |

---

## 2. Änderungslog-Einträge

| Fix-Titel | Befund | Satz-Befund |
|---|---|---|
| ¹ `1a#1` (Doppelte Focus-Rings) | ✅ | Der native Focus-Ring wird über `focus-visible:outline-none` unterdrückt und durch den 2px Brand Blue Ring (`focus-visible:ring-2 focus-visible:ring-accent-alt`) ersetzt; die Signatur ist stabil und die Rückgabe ist ein Superset. |
| ² `1a#2` (Fehlende @theme-Keys) | ✅ | Die 6 Tokens wurden unter `--spacing-*` und `--radius-*` registriert und über die Smoke-Test-Datei `mpz-studio-theme-build-smoke.ts` im Tailwind Build-Prozess validiert. |
| ³ `1a#3` (Wirkungsloser Stack-Helfer) | ✅ | `mpzStackClassName` gibt das Layout-Modell (`flex flex-col`) zusammen mit der `gap-*`-Klasse zurück. |
| ⁴ `1b#1` (Card-Radius 8 vs. 12) | ✅ | Der geplante Visual-Change (Wechsel auf 8px Radius bei Consumer-Migration ab #207) wurde im Plan dokumentiert und verbleibt planmäßig außerhalb des Codes von #206. |
| ⁵ `1b#2` (Nav-Active-Stripe-Farbe) | ✅ | Die Sidebar-Navigation (`mpzSidebarNavItemClassName`) wurde mit `border-accent` (grün) und `border-l-4` für den aktiven Zustand ausgestattet. |
| ⁶ `1b#3` (Validation-Card-Vertrag) | ✅ | Die `validation`-Variante von `MpzCard` nutzt ausschließlich `border-l-4 border-l-accent`, ohne Background-Tint. |
| ⁷ `1b#4` (Sidebar 240 vs. 256) | ✅ | Der Sidebar-Breite-Token wurde auf `240px` festgelegt und im CSS hinterlegt. |
| ⁸ Offene Frage 1 (Card default ohne Shadow, kein speculative elevated) | ✅ | Die Card hat in der Standard-Variante keinen Shadow, und es wurde kein spekulatives `elevated`-Prop implementiert. |
| ⁹ Offene Frage 2 (Variablen in CSS, Klassen in TSX) | ✅ | CSS-Variablen liegen in `.css`-Dateien; die HTML-Klassen werden programmatisch in TSX zusammengestellt. |
| ¹⁰ Offene Frage 3 (Stammdaten-Form bleibt in #211) | ✅ | Keine Consumer-Migration durchgeführt, der Querschnitt wurde als Superset rückwärtskompatibel implementiert. |

---

## 3. Entscheidungstabelle

Der implementierte Code entspricht in allen Punkten der Entscheidungstabelle des Plans. Alle wesentlichen Gestaltungsregeln und architektonischen Leitlinien (wie die Verwendung von dimensionalen Tokens in `--spacing-*` zur Kompatibilität mit Tailwind v4-Utilities) wurden exakt befolgt.

---

## 4. Scope

Folgende Änderungen im Code weichen vom Plan ab oder gehen über ihn hinaus:
1. **Zusätzliche Smoke-Test-Hilfsdatei (`app/components/mpz-studio/mpz-studio-theme-build-smoke.ts`):** 
   - **Risiko:** Keines. Die Datei exportiert eine statische String-Konstante mit den Klassen der sechs registrierten Tokens.
   - **Empfehlung:** Beibehalten. Dies realisiert die im Plan unter "Fehlercodes & Validierungsregeln" und "Implementierungsschritte 6" geforderte Build-Smoke-Prüfung, ohne dass dafür echte UI-Komponenten mit Dummy-Klassen überladen werden müssen.
