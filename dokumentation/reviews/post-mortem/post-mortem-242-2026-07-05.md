---
tags:
  - post-mortem
  - 04-post-mortem
  - issue-242
  - hsts-proxy
erstellt: 2026-07-05
plan: .cursor/plans/hsts_proxy_#242_9e537be9.plan.md
---

# Post-Mortem 4 — Plan-Code-Abgleich nach Implementierung (HSTS Proxy #242)

Schlusskontrolle: Hat die Implementierung den gehärteten Plan tatsächlich umgesetzt? (Abgleich Plan `hsts_proxy_#242_9e537be9.plan.md` gegen den aktuellen Code- und Dokumentationsstand).

## 1. Todos

Alle Todos aus dem Frontmatter des Plans wurden vollständig umgesetzt (Doku-Sync verifiziert).

| id | Befund |
|---|---|
| `verify-baseline` | ✅ |
| `coolify-hsts` | ✅ |
| `coolify-hsts-legacy` | ✅ |
| `verify-prod` | ✅ |
| `docs-sync` | ✅ |
| `close-issue` | ✅ |

## 2. Änderungslog-Einträge

Alle Pre-Mortem-Fixes aus dem Änderungslog wurden exakt umgesetzt und sind in der Entwickler-Doku (`anleitungen/fuer-entwickler.md`) persistent verankert.

| Fix-Titel | Status | Befund |
|---|---|---|
| ¹ 1b #1 (Middleware-Overwrite) | ✅ | Das Anhängen der Liste (`gzip,sn-hsts@file`) ist in der Doku vermerkt. |
| ² 1b #4 (`forceSTSHeader`) | ✅ | Verbot von `forceSTSHeader` ist dokumentiert (RFC-Konformität). |
| ³ 1b #2 (Dynamic-Config-Fallback) | ✅ | Pfad und File-Provider-Snippet sind in der Doku festgehalten. |
| ⁴ 1b #3 (Doku-Mirror-Widerspruch) | ✅ | Beide Domains (GS39 und Legacy) sind abgedeckt und dokumentiert. |
| ⁵ Fragen 2 & 3 (Router-Name, Zugang) | ✅ | Explizites Ablesen der Labels und Dynamic-Config-Weg dokumentiert. |

## 3. Entscheidungstabelle

Die Implementierung und Dokumentation entspricht vollständig den getroffenen Architekturentscheidungen. Keine Abweichungen bei Typen, Headern oder Fallbacks. HSTS wird wie gewollt nur am Proxy (Coolify/Traefik) gesetzt, die Next.js-Security-Header blieben unangetastet.

## 4. Scope

Plan und Doku stimmen vollständig überein. Kein App-Code geändert (bewusst Proxy-only). Umsetzung vertragstreu zum gehärteten Plan.

## 5. Prod-Verifikation (2026-07-05)

| Prüfung | Ergebnis |
|---------|----------|
| `https://39-gs.mpz.schule/` | `strict-transport-security: max-age=15552000` |
| `/scan`, `/raum/musik`, `/api/health` | HSTS vorhanden |
| `http://39-gs.mpz.schule/` | kein HSTS (RFC 6797) |
| `https://schulnavigator.mpz.schule/` | HSTS vorhanden |
| CSP, Permissions-Policy | unverändert |
| Router-Middlewares | `gzip,sn-hsts@file` (beide Applications) |

**Issue:** [#242](https://github.com/flxln/schulnavigator/issues/242) geschlossen.
