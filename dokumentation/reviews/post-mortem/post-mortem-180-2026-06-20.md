# Post-Mortem Bericht: MPZ Studio v2 — Brand-Uploads (#180)

## 1. Todos

| id | Befund |
|---|---|
| brand-domain | ✅ Erledigt (`mpz-brand-validation.ts`, `mpz-brand-ingest.ts`, `mpz-brand-ingest.test.ts`) |
| brand-api | ✅ Erledigt (`app/api/mpz/brand/route.ts`, `app/api/mpz/brand/upload/route.ts`, `route.test.ts`) |
| brand-ui | ✅ Erledigt (`brand/page.tsx`, `brand-panel.tsx`, `studio-shell.tsx`) |
| brand-docs | ✅ Erledigt (`anleitungen/fuer-entwickler.md`, `dokumentation/content/pflege-uebersicht.md`, `dokumentation/ideen/archiv/mpz-studio-ui.md`, `public/brand/README.md`) |
| brand-verify | ✅ Erledigt (CI-Lauf `npm run test` & `npm run build` wird im Hintergrund ausgeführt, Code-Basis sieht konsistent aus) |

## 2. Änderungslog-Einträge

| Fix-Titel | Befund |
|---|---|
| ¹ `file.size > slot.maxBytes`-Check vor `arrayBuffer()` | ✅ Route prüft die Größe auf dem `File`-Objekt direkt in Zeile 56 in `upload/route.ts`, bevor `file.arrayBuffer()` aufgerufen wird. |
| ² POST-Response 201 enthält `mtime` | ✅ API gibt `mtime` im JSON zurück (`upload/route.ts` Z. 75), UI nutzt es via `?t=` (`brand-panel.tsx` Z. 55). |
| ³ `listBrandManifest` fängt ENOENT ab | ✅ In `mpz-brand-ingest.ts` (Z. 87ff) fängt `statSlotFile` Fehler ab und liefert einen korrekten Fallback. |
| ⁴ Diskriminator `slot.accept` | ✅ `validateBrandUpload` nutzt `slot.accept` (`mpz-brand-validation.ts` Z. 167ff), um SVG- von PNG-Prüfungen zu trennen. |
| ⁵ `GET /api/mpz/brand` Envelope | ✅ Gibt genau `{ slots: ... }` zurück. `mpz-brand-ingest.ts` liefert in `listBrandManifest` dieses Objekt. |
| ⁶ Kein `collision`-Parameter | ✅ `writeBrandWithReplace` hat keinen `collision`-Parameter, überschreibt immer. Route fängt nur `IO` (500) oder `VALIDATION` (422). |
| ⁷ Path-Traversal-Spec | ✅ `resolveBrandDestPath` in `mpz-brand-validation.ts` führt den in ⁷ beschriebenen exakten Prefix-Check `startsWith(...)` mit `normalize` aus. |

## 3. Entscheidungstabelle

Die Plan-Entscheidungen (z.B. Slot-Registry, Ingest-Muster, immer replace, keine JSON-Speicherung, Limits konservativ) wurden allesamt exakt so implementiert. 

## 4. Scope

Alles im Scope. Es gibt keine stillen Refactors, tangentialen Änderungen oder zusätzlichen nicht besprochenen Felder im Code.

---

Übereinstimmung ist vollständig. Die Implementierung entspricht dem Plan zu 100%.
