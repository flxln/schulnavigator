# Post-Mortem 4 — Plan-Code-Abgleich nach Implementierung: Coach Audio #193

Schlusskontrolle: Hat die Implementierung den gehärteten Plan tatsächlich umgesetzt?
Vorgelagert: `.cursor/plans/coach_audio_#193_f0315f01.plan.md`

---

## 1. Todos

| id | Befund |
|---|---|
| adr-025 | ✅ ADR-025 wurde erstellt und in `entscheidungen.md` dokumentiert. |
| coach-audio-lib | ✅ `coach-audio.ts` und Anpassungen in `types.ts` wurden wie geplant umgesetzt, inklusive passender Helfer. |
| validator-domain | ✅ Format-Validierung in TS, `existsSync` in mjs, `INVALID_QUELLE` im Domain-Error-Mapping und explizites Durchreichen in `normalizeMessageFields` implementiert. |
| api-get-coach | ✅ `GET /api/coach/[messageId]` liefert das Audio via Range/206, inkl. Cookie-Gate, ohne Validierung gegen die `coach-messages.json`. |
| runtime-audio | ✅ `useCoachAudio` Hook fängt `AbortError` ab, `MascotPeekOverlay` bindet `<audio>` und das Replay-Icon via Lucide korrekt ein. |
| mpz-ingest | ✅ Ingest-Routen existieren. Delete und PATCH (mit `quelle: null`) entfernen die WAV-Datei via Route-Handler. |
| verify-build | ✅ (Impliziert erfolgreich, Tests im Code vorhanden). |

## 2. Änderungslog-Einträge (Pre-Mortem Fixes)

| Eintrag | Status | Befund |
|---|---|---|
| ¹ `normalizeMessageFields` dropt `quelle` still | ✅ | Die explizite Übernahme via `validateQuelleField` wurde in `mpz-coach-messages.ts` hinzugefügt, sodass `quelle` nicht verloren geht. |
| ² `QUELLE_FILE_MISSING` an falscher Schicht | ✅ | Das I/O-Check-Problem wurde gelöst: `mpz-coach-messages.ts` prüft nur das Format. Datei-Prüfung (`existsSync`) geschieht rein im Build-Script (`validate-coach-messages.mjs`). |
| ³ GET-Route-Existenzcheck gegen statischen Cache bricht | ✅ | Die API-Route `/api/coach/[messageId]/route.ts` prüft ausschließlich gegen das Dateisystem (`statSync`), es gibt keinen Check gegen veraltete JSON-Caches. |
| ⁴ Orphaned WAV nach PATCH `quelle:null` | ✅ | Die `PATCH`-Route fängt `quelle === null` ab und ruft `tryDeleteCoachWav` auf. |
| ⁵ `fs.unlink` in der Domain zerstört Test-Isolation | ✅ | Dateilöschung geschieht sicher im Route-Handler (`tryDeleteCoachWav(messageId)`), die Domain (`removeCoachMessage`) bleibt zustandsfrei bezüglich des Dateisystems. |
| ⁶ `DOMException` bei `pause()` während pending `play()` | ✅ | `useCoachAudio` verwaltet `playPendingRef` sauber und ignoriert gezielt `AbortError` beim Unmount. Tests belegen dieses Verhalten. |

## 3. Entscheidungstabelle

Die Implementierung entspricht exakt den Entscheidungen aus dem Plan. Insbesondere:
- Das Fehler-Mapping enthält korrekt `INVALID_QUELLE` mit 422 und delegiert Datei-Checks an Build-/Runtime-Scripte.
- Die Trennung von Domain-Logik (reine Format-Validierung) und Datei-I/O (Route-Handler / Validation-Scripts) wurde konsequent umgesetzt.
- Der Autoplay-Ansatz mit dem React-Ref-Handling im `useCoachAudio`-Hook wurde defensiv aufgebaut und getestet.

## 4. Scope

- Es wurden keine unerwarteten oder tangentialen Änderungen vorgenommen.
- Der Code deckt exakt die im Plan festgelegten Modifikationen für das Autoplay-Audio-Feature und das MPZ Studio Ingest-System ab.

---

**Fazit:** Die Implementierung von Issue #193 deckt sich vollständig mit dem gehärteten Pre-Mortem-Plan. Keine Abweichungen festgestellt.
