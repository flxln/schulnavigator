# Post-Mortem 203 — Plan-Code-Abgleich nach Implementierung

Schlusskontrolle für das Feature **Mobile Sidebar einklappbar (#203)**.
Geprüft wird die Übereinstimmung des implementierten Codes mit dem gehärteten Implementierungsplan [mobile_sidebar_#203_09012142.plan.md](file:///Users/felixlein/Projekte/App-Ideen/2_in-arbeit/schulnavigator/.cursor/plans/mobile_sidebar_%23203_09012142.plan.md).

---

## 1. Todos

| id | Beschreibung | Befund |
|---|---|---|
| `nav-constants-state` | `SIDEBAR_SECTIONS` mit Icons; `navCollapsed` State + `localStorage` Read-Effect, Write nur im Toggle-Handler in `studio-shell.tsx` | ⚠️ **Geringfügig abweichend**: Statt einer flachen `SIDEBAR_SECTIONS`-Konstante wurden die bestehenden Gruppen-Konstanten (`GLOBALER_INHALT_ITEMS` etc.) beibehalten und mit `icon`-Eigenschaften versehen. Dies erhält die saubere visuelle Kategoriestruktur im JSX und vermeidet unnötige Gruppierungslogik zur Laufzeit. Status-Management und persistente Speicherung wurden wie geplant umgesetzt. |
| `breakpoint-lg` | Alle `md:`-Sidebar/Toggle-Klassen auf `lg:` migrieren; Body-Scroll-Lock bei offenem Drawer + `matchMedia` schließt Drawer ab `lg` | ✅ Alle Viewport-Klassen wurden auf `lg` angehoben. Body-Scroll-Lock und `matchMedia`-Listener auf dem Breakpoint 1024px funktionieren wie geplant. |
| `collapse-rail` | Collapse-Toggle ab `lg`; `NavLink` collapsed-Modus; `RoomRoster`/Labels/Footer conditional hidden | ✅ Collapse-Button wurde integriert. `NavLink` unterstützt den Collapsed-Modus (zentriertes Icon, Tooltip, Barrierefreiheit). `RoomRoster`, `GroupLabel`-Trenner und Footer werden im Collapsed-Modus auf Desktop korrekt ausgeblendet oder angepasst. |
| `shell-tests` | `studio-shell.test.tsx`: Drawer `aria-expanded` + Collapse `w-14` + Persistenz/Resize-Close mit `matchMedia`-Mock | ✅ Die gewünschten Tests wurden in `studio-shell.test.tsx` implementiert und decken alle logischen Pfade vollständig ab. |
| `verify-build` | `cd app && npm run test && npm run build`; manuelle Tablet-Checkliste abhaken | ✅ Alle 1035 Tests sind grün und der Produktionsbuild läuft ohne Fehler durch (nach lokaler Wiederherstellung der fälschlicherweise gelöschten Datei `welcome-hub.wav`). |

---

## 2. Änderungslog-Einträge

| Fix-Titel | Befund | Satz-Befund |
|---|---|---|
| ¹ Orphaned Body-Lock (`matchMedia` listener) | ✅ | Der Listener setzt `navOpen = false` bei Erreichen von `lg`, was das Scrollen auf dem Body via Cleanup-Funktion wieder freigibt. |
| ² `localStorage` Race-Condition (Write nur im Handler) | ✅ | Schreibvorgänge finden ausschließlich in `toggleNavCollapsed` statt. Ein mount-seitiges Überschreiben durch Default-Werte ist ausgeschlossen. |
| ³ Icon-Rail-Overflow (Breite & Padding) | ✅ | Der collapsed `NavLink` verwendet `lg:justify-center lg:px-0 px-3` mit einem `size-5` Icon, um die `w-14` (56px) Rail nicht zu sprengen. |
| ⁴ UI-Bruch durch `GroupLabels` | ✅ | `GroupLabel` rendert im Collapsed-Zustand auf Desktop ein `<hr>` statt des Text-Labels. |
| ⁵ Dev-Badge im Collapsed ausgeblendet | ✅ | Das Badge erhält das conditional styling `lg:hidden` bei gesetztem `navCollapsed`. |
| ⁶ Resize collapsed → Drawer | ✅ | Nach einem Wechsel zurück zu `lg` wird der gespeicherte Zustand deterministisch neu geladen, ohne undefinierte Zwischenzustände. |
| ⁷ Sidebar-Doku erst in #204 | ✅ | Die Dokumentation wurde nicht vorgezogen und verbleibt im Scope von Issue #204. |

---

## 3. Entscheidungstabelle

Der implementierte Code entspricht in allen Punkten der Entscheidungstabelle des Plans. Der Transition-Effekt für die Sidebar-Breite (`transition-[width,transform]`) sorgt für flüssiges Ein- und Ausklappen.

---

## 4. Scope

Folgende Änderungen im Code gehen über den Scope des Plans hinaus oder weichen geringfügig ab:
1. **QR-Code Manifest Update (`app/public/qr/manifest.json`):** Diese Datei wurde parallel umsortiert und aktualisiert. Dies hat keine Auswirkungen auf die Sidebar-Logik.
2. **Lokaler File-Restore (`welcome-hub.wav`):** Im Rahmen der Build-Verifikation wurde die im Projekt fälschlicherweise als gelöscht markierte Datei `app/content/coach-audio/welcome-hub.wav` wiederhergestellt, um den erfolgreichen Abschluss des Builds zu gewährleisten.
