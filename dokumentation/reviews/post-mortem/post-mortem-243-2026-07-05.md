# Post-Mortem #243 — Headscale NAS Backup (2026-07-05)

**Issue:** [#243](https://github.com/flxln/schulnavigator/issues/243)  
**Plan:** [headscale_nas_backup_#243_2548ea44.plan.md](../../.cursor/plans/headscale_nas_backup_#243_2548ea44.plan.md)  
**Pre-Mortems:** pre-mortem-1a-headscale-nas-243-2026-07-05.md · pre-mortem-1b-headscale-nas-243-2026-07-05.md

---

## Ergebnis

**Go** — Phase-1-Backup VPS → NAS über Headscale umgesetzt. Erster Voll-Lauf erfolgreich: ~87 min, ~2,8 GB, Exit 0.

---

## Tailnet-IPs (ohne Secrets)

| Knoten | User | IP |
|--------|------|----|
| VPS-Host | `mpz-vps@headscale` | `100.64.0.7` |
| NAS DS218+ | `schulnavigator-nas@headscale` | `100.64.0.9` |
| Altlast (bereinigen) | userspace-Test | `100.64.0.8` offline |

---

## 1. Todos

| id | Inhalt | Befund |
|----|--------|--------|
| triage-done | Triage abgeschlossen | ✅ |
| step-0-plan | Plan + SSOT-Anleitung | ✅ |
| step-1a-premortem | Pre-Mortem 1a | ✅ |
| step-1b-premortem | Pre-Mortem 1b | ✅ |
| step-2-haerten | Plan gehärtet | ✅ |
| vps-tailscale | VPS `mpz-vps` → `100.64.0.7` | ✅ |
| headscale-users-acl | NAS-User + ACL deployt | ✅ |
| nas-tailscale | Tailscale-SPK, `tailscale up` | ✅ |
| backup-read-user | `backup-read` + rrsync + POSIX-ACL | ✅ |
| rsync-cron | Skript deployed, erster Lauf OK | ✅ (Cron DSM manuell) |
| step-4-postmortem | Post-Mortem | ✅ |
| verify-close-243 | Hash-Check + `gh issue close` | ✅ |

---

## 2. Änderungslog-Einträge (¹–⁹)

| # | Fix | Befund |
|---|-----|--------|
| ¹ | docker exec statt bare headscale | ✅ Alle Headscale-Befehle via Container-Exec |
| ² | ssh-keygen auf NAS + Verify mit NAS-Key | ⚠️ Key auf Mac erzeugt und auf NAS kopiert; Verify lief korrekt mit NAS-Key — Sicherheitsziel erfüllt, Reihenfolge abweichend |
| ³ | Exit-Code-Sammlung statt `set -e` | ✅ Skript fängt pro Verzeichnis, `exit "${status}"` am Ende |
| ⁴ | Dienstname `pkgctl-Tailscale.service` vorab bestätigen | ⚠️ Nicht explizit per `synosystemctl list-units` geprüft; SPK startet korrekt mit diesem Namen |
| ⁵ | Bedingter `mkey:`-Zweig | ✅ nicht benötigt — Pre-Auth-Key hat direkt funktioniert |
| ⁶ | POSIX-ACLs inkl. Default-ACL | ✅ `setfacl -R` + `setfacl -R -d`; ACL-Test mit frischer Datei OK |
| ⁷ | Hash-Check statt Schreib-Restore | ✅ rsync-Pull + sha256sum; Notfall-Restore-Weg über Admin dokumentiert |
| ⁸ | Btrfs Snapshot Replication | ⚠️ offen — DSM Aufgabenplanung manuell |
| ⁹ | rrsync Forced-Command + relative Pfade | ✅ `rrsync -ro /data/schulnavigator/`; Skript mit relativen Pfaden |

---

## 3. Entscheidungen

| # | Entscheidung | Befund |
|---|-------------|--------|
| E1 | POSIX-ACLs inkl. Default-ACL | ✅ `setfacl -R` + `-d`; `acl`-Paket auf VPS nachinstalliert |
| E2 | Hash-Check vom NAS; Notfall-Restore per Admin | ✅ dokumentiert |
| E3 | `rrsync -ro` Forced-Command | ✅ + Fix: Shell `/bin/sh` (nicht `nologin`) erforderlich |
| E4 | Btrfs Snapshot Replication, 30 Tage | ⚠️ offen |
| E5 | Exit-Code-Sammlung, Benachrichtigung bei Fehler | ✅ im Skript; DSM-Task-Benachrichtigung manuell |

---

## 4. Scope (nicht im Plan)

- **`acl`-Paket** auf dem VPS nicht installiert → nachinstalliert (einmalig)
- **DSM 7 userspace-networking** erfordert `tailscale nc` ProxyCommand → im Skript dokumentiert; `configure-host`-Script auf NAS deployed
- **SSH-Port 2222** auf NAS (Plan: Port 22) — SSH-Config angelegt (`~/.ssh/config`)
- **`felixlein`** statt `admin` als NAS-Account (kein `admin`-Home aktiv); Shell `/bin/sh` auf `backup-read` nötig
- **Setup-Skript** (`headscale-nas-setup.sh`) als Workaround für non-interactive sudo → Pre-Auth-Key eingebettet (temporär, löschen)
- **Node-Altlast** `100.64.0.8` (userspace-Test) in Headscale offline

---

## Verifikation

**Größenabgleich (2026-07-05):**

| Pfad | VPS | NAS |
|------|-----|-----|
| `media/` | 2,8 GB | 2,8 GB |
| `dialog-audio/` | 16 MB | 16 MB |
| `coach-audio/` | 6,9 MB | 6,9 MB |

**Hash-Integrität (E2):** rsync-Pull + `sha256sum` — `media/README.md` ✅, `coach-audio/halfway.wav` ✅, `dialog-audio/daz/01-03-*.wav` ✅

---

## Offene Ops (nicht blockierend für #243)

- [ ] DSM Aufgabenplanung: Boot-Task `configure-host` + `pkgctl-Tailscale.service` (root) → [#246](https://github.com/flxln/schulnavigator/issues/246)
- [ ] DSM Aufgabenplanung: nightly `~/bin/nas-backup-rsync.sh`, Benachrichtigung bei abnormaler Beendigung → [#246](https://github.com/flxln/schulnavigator/issues/246)
- [ ] Btrfs Snapshot Replication (`schulnavigator-backup`, täglich, 30 Tage) → [#247](https://github.com/flxln/schulnavigator/issues/247)
- [ ] Optional: Shared Folder `/volume1/schulnavigator-backup` (statt Home-Pfad)
- [ ] `rm ~/headscale-nas-setup.sh` auf NAS (Pre-Auth-Key entfernen) → [#246](https://github.com/flxln/schulnavigator/issues/246)
- [ ] Node `100.64.0.8` in Headscale löschen → [#248](https://github.com/flxln/schulnavigator/issues/248)

---

## Referenzen

- [backup-t5-nas-headscale.md](../planung/backup-t5-nas-headscale.md)
- [nas-backup-rsync.example.sh](../planung/scripts/nas-backup-rsync.example.sh)
- [nas-headscale-configure-host.example.sh](../planung/scripts/nas-headscale-configure-host.example.sh)
- [vps-backup-read-setup.example.sh](../planung/scripts/vps-backup-read-setup.example.sh)
- [anleitung_headscale_synology_ds218_schulnavigator.md](~/Projekte/MPZ%20-%20Headscale/anleitungen/anleitung_headscale_synology_ds218_schulnavigator.md)
