# Post-Mortem #163 — Dialog-Audio-Tab pro Station
**Datum:** 2026-06-18
**Plan:** `mpz_studio_#163_dialog-audio_ea5e7fc5.plan.md`
**Berührte Dateien geprüft:**
- `app/components/mpz-studio/dialog-audio-panel.tsx`
- `app/components/mpz-studio/station-detail-shell.tsx`
- `app/components/mpz-studio/station-detail-shell.test.tsx`

---

## 1. Todos

| id | Befund |
|----|--------|
| `panel-slug-prop` | ✅ Umgesetzt: `DialogAudioPanelProps = { slug?: string }`, `slug: slugProp` destrukturiert, abgeleiteter Slug `const slug = slugProp ?? selectedSlug`, `<select>` strikt auf `slugProp === undefined` begrenzt, kein Sync-`useEffect`. |
| `shell-wiring` | ✅ Umgesetzt: Import `DialogAudioPanel` in Z. 8; Platzhalter ersetzt durch `<DialogAudioPanel slug={slug} />` in `<section>` (Z. 165–169); Render-Guard `activeTab === 'dialog-audio' && hasDialog` vorhanden. |
| `shell-test` | ✅ Umgesetzt: `vi.stubGlobal('fetch', ...)` in neuem asynchronen Test (Z. 113–150); `vi.unstubAllGlobals()` im `afterEach` (Z. 83); Test nutzt `await screen.findByText('Clip')`, nicht `getByText`. |
| `verify` | Kann hier nicht maschinell bestätigt werden — muss vom Entwickler via `npm run test` + `npm run build` verifiziert werden (kein Code vorhanden, der darauf hindeutet, dass es fehlgeschlagen ist). |

---

## 2. Änderungslog-Einträge

| Fix | Befund | Satz |
|-----|--------|------|
| ¹ 1a #1: Abgeleiteter Slug statt `useState(slugProp ?? 'daz')` | ✅ | `const slug = slugProp ?? selectedSlug` in `dialog-audio-panel.tsx` Z. 40 exakt wie im Plan spezifiziert. |
| ² 1a #2: Sync-`useEffect` gestrichen | ✅ | Kein Sync-`useEffect` im Code; einziger `useEffect` ist der bestehende `[slug, loadStatus]`-Effekt (Z. 65–67). |
| ³ 1a #3: `fetch`-Mock via `vi.stubGlobal` + Cleanup | ✅ | `vi.stubGlobal('fetch', ...)` im neuen Test (Z. 118); `vi.unstubAllGlobals()` im `afterEach` (Z. 83). |
| ⁴ 1b #3: Asynchroner Test mit `findBy`/`waitFor` | ✅ | Test verwendet `await screen.findByText('Clip')` (Z. 147) statt synchronem `getByText`. |
| ⁵ 1b #1: Status-Route-Tabelle korrigiert (`error: err.code`) | ✅ (Doku-Fix) | Keine Code-Änderung nötig — der Fix betraf die Plan-Dokumentation, nicht den implementierten Code. Verifizierbar in `status/route.ts` (nicht geändert). |
| ⁶ 1b #2: Bekannte Limitation `hasDialog ?? false` dokumentiert | ✅ (Doku-Fix) | Verhalten im Code korrekt: `const hasDialog = summary?.hasDialog ?? false` (shell Z. 52); in Scope-Abgrenzung akzeptiert. |
| ⁷ Offene Fragen entschieden | ✅ | Tab-Badge: nicht im Code. Dynamische `DIALOG_SLUGS`: nicht geändert. Konventions-Hinweis: nicht im Detail-Tab. Alle drei Entscheidungen konsequent umgesetzt. |

---

## 3. Entscheidungstabelle

| Entscheidung | Code entspricht? | Anmerkung |
|---|---|---|
| `slug` als optionale Prop statt separatem Komponenten | ✅ | `DialogAudioPanelProps = { slug?: string }` (Z. 33–35), einheitlicher Komponent. |
| Keine neuen API-Routen | ✅ | Nur bestehende Fetch-Calls auf `/api/mpz/dialog-audio/status` und `/ingest`. |
| Dropdown nur ohne `slugProp` | ✅ | `{slugProp === undefined && (<div>...<select>...)}` (Z. 132–151). |
| Tab-Sichtbarkeit via `hasDialog` aus Validation-Report | ✅ | `hidden: !hasDialog` im Tabs-Array (Z. 67); Render-Guard in JSX (Z. 165). |
| Konventions-Hinweis nur global | ✅ | Kein Hinweis im Detail-Tab; globale `page.tsx` unverändert. |
| Effektiver Slug abgeleitet, kein State-Shadowing | ✅ | `const slug = slugProp ?? selectedSlug` (Z. 40); `selectedSlug`-State steuert nur den `<select>`. |
| Tab-Badge `missingCount`, dynamische `DIALOG_SLUGS` | ✅ (Nicht-Umsetzung) | Beide bewusst ausgelassen; Code enthält keine entsprechenden Erweiterungen. |
| `collision: 'replace'` im Upload | ✅ | `form.set('collision', 'replace')` (Z. 80). |
| `applyReport` nach Upload | ✅ | `applyReport(json.validation, json.mtime ?? json.validation.stationsModifiedAt)` (Z. 100). |
| Fehlertypen: `loadError` / `rowError` | ✅ | Beide States vorhanden (Z. 42–44); Anzeige via `json.message` oder generischem Text (Z. 55, 95). |
| `COLLISION` tritt im UI-Pfad praktisch nicht auf | ✅ | Stimmt: `collision: 'replace'` fest gesetzt; kein UI-Code behandelt `COLLISION` separat. |

---

## 4. Scope

Folgendes ist im Code vorhanden, steht aber nicht explizit im Plan:

- **`vi.unstubAllGlobals()` im `afterEach` gemeinsam mit bestehendem `cleanup()` und `mocks`-Reset** (shell.test Z. 79–84): Das `afterEach` wurde konsolidiert — `vi.unstubAllGlobals()` steht in dem bereits vorhandenen `afterEach`-Block zusammen mit `cleanup()` und den Mock-Resets, statt in einem separaten Block. Technisch korrekt, Cleanup-Reihenfolge unbedenklich.

- **Vollständiges Segment-Objekt im `fetch`-Mock** (shell.test Z. 124–136): Der Mock enthält alle Felder von `DialogSegmentAudit` (`segmentId`, `rolle`, `textPreview`, `expectedClip`, `quelle`, `fileExists`, `quelleMatchesConvention`, `state`). Der Plan spezifiziert nur `/* min. 1 Segment */` als Kommentar — die vollständige Ausformulierung ist eine sinnvolle Konkretisierung, kein Scope-Bruch.

- **Test prüft auch `'01-frieda.wav'`** (shell.test Z. 149): Zusätzlich zu Tabellen-Headern `Clip`/`Status` wird der konkrete Clip-Name verifiziert. Erhöht Testtiefe, kein Risiko.

- **Kein optionaler `dialog-audio-panel.test.tsx`**: Die im Plan als „Optional (nicht Pflicht für #163)" markierte Unit-Test-Datei wurde nicht angelegt. Kein Abweichungsrisiko, da explizit optional.

---

Alle vier Pflicht-Todos entsprechen dem Plan. Sämtliche Härtungs-Fixes (¹–⁷) sind korrekt umgesetzt. Keine Abweichung erfordert Nacharbeit am Code oder am Plan.
