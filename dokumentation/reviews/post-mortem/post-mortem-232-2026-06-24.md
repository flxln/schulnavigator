---
tags:
  - post-mortem
  - issue-232
  - schuelermedien-deploy-trennung
  - git-history
erstellt: 2026-06-24
---

# Post-Mortem #232 — Git-History: Schüler-Medien aus LFS/GitHub entfernen

**Plan:** `.cursor/plans/issue_232_git-history_e35f3875.plan.md`  
**Abgleich:** Technische Phasen A–D, D.5, F; Phase E (GitHub Support) als Nacharbeit dokumentiert.

---

## 1. Verifikation (V1–V10)

| ID | Ergebnis | Notiz |
|----|----------|-------|
| V1 | ✅ | O3 + DSB-Bewertung in [05-offene-punkte.md](../../planung/schuelermedien-deploy-trennung/05-offene-punkte.md) (#232-Abschnitt) |
| V2 | ✅ | Mirror `/tmp/schulnavigator-pre-232-mirror.git`, `git lfs fetch --all` (35 Objekte) |
| V3 | ✅ | `git filter-repo --analyze` — nur Bahn-B-Pfade entfernt |
| V4 | ✅ | `git rev-list --objects --all` — keine Bahn-B-Objekte außer `.gitkeep` |
| V5 | ✅ | `git lfs ls-files --all` — nur `app/public/stations/**` (17) |
| V6 | ✅ | `6a55156:app/public/media/...` nicht mehr erreichbar |
| V7 | ✅ | `npm run build` auf `kunde/39-gs` nach Rewrite grün |
| V8 | ✅ | `GET https://39-gs.mpz.schule/api/health` → 200 |
| V9 | ⏳ | GitHub-Support-Ticket ausstehend — Vorlage [08-github-support-ticket-232.md](../../planung/schuelermedien-deploy-trennung/08-github-support-ticket-232.md) |
| V10 | ✅ | Pre-Baseline `pre-rewrite-refs.txt` (40 Refs); Post-Rewrite keine Bahn-B-Commits |

---

## 2. Durchgeführte Schritte

1. **Mirror-Backup:** `git clone --mirror` + `git lfs fetch --all`
2. **Rewrite:** `git filter-repo --force --invert-paths` für `app/public/media/`, `app/content/dialog-audio/`, `app/content/coach-audio/`
3. **Force-Push:** `git push --force origin --all` und `--tags` (6 Branches, Tag `pre-adr-016`)
4. **D.5:** `.gitkeep` auf `kunde/39-gs` (`33bc097`) und `main` (`980939a`)
5. **Lokal:** `git lfs prune --verify-remote` im Post-232-Klon

**Backup (MPZ-only, nicht pushen):** `/tmp/schulnavigator-pre-232-mirror.git` — enthält Pre-Rewrite-History inkl. LFS.

---

## 3. Geänderte Remote-Refs (Auszug)

| Branch/Tag | Vorher (Auszug) | Nachher (Auszug) |
|------------|-----------------|------------------|
| `kunde/39-gs` | `ee0df4a` | `33bc097` (inkl. gitkeep) |
| `main` | `8e5ffc4` | `980939a` |
| `pre-adr-016` | `a891170` | `a832773` |

Vollständige SHA-Zuordnungen: `.git/filter-repo/changed-refs` im Mirror (vor Push).

---

## 4. Offene Nacharbeit

| Punkt | Verantwortlich |
|-------|----------------|
| GitHub-Support: LFS-Purge + `refs/pull/*` | MPZ-Admin — [08-github-support-ticket-232.md](../../planung/schuelermedien-deploy-trennung/08-github-support-ticket-232.md) |
| Alle lokalen Klone verwerfen und neu klonen | MPZ-Team |
| Coolify-Redeploy `kunde/39-gs` prüfen (Push ausgelöst) | MPZ |

---

## 5. Fazit

History-Rewrite und Force-Push abgeschlossen. Schüler-Medien sind in Git-Objekten und LFS-Index aller gepushten Branches nicht mehr rekonstruierbar. V9 (GitHub-seitiger LFS-Purge) bleibt bis Support-Bestätigung offen — blockiert den laufenden Betrieb nicht (Medien auf Hetzner-Volumes).
