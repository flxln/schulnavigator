# Post-Mortem — Issue #229 Phase 2 (Coolify-Volumes & Structure-Validatoren)

**Datum:** 24. Juni 2026  
**Issue:** [#229](https://github.com/flxln/schulnavigator/issues/229) — Phase 2: Server, Coolify-Volumes & Structure-Validatoren  
**Zweig:** `kunde/39-gs`  
**Plan:** `.cursor/plans/issue_229_phase-2_24ed5ebf.plan.md`

---

## 1. Todos (Plan)

| ID | Inhalt | Befund |
|---|---|---|
| `structure-validators` | `validate:stations:structure`, `validate:coach:structure`, `build` umgestellt | ✅ Umgesetzt |
| `test-fixture-repair` | Tests von Live-`klassenzimmer` entkoppelt | ✅ Umgesetzt (1140 Tests grün) |
| `local-build-verify` | Build ohne Bahn-B simuliert | ✅ Umgesetzt |
| `docs-coolify-volumes` | `fuer-entwickler.md` Volume-Abschnitt | ✅ Umgesetzt |
| `ionos-vps-volumes` | Host-Pfade + chown 1001 | ✅ Ops auf IONOS-VPS |
| `coolify-mounts` | Drei Bind-Mounts Prod | ✅ Coolify DB + Redeploy |
| `initial-rsync` | Medien ~742M + Dialog + Coach | ✅ rsync mit `sudo rsync` |
| `prod-smoke` | Video, Dialog, Coach, stations-icons | ⚠️ Offen bis Coolify-Redeploy auf `kunde/39-gs` (`main` eingefroren) |

---

## 2. Zusätzlicher Fix (nicht im Plan)

**Next.js Standalone + Volume-Mount:** Statische `public/media`-Dateien werden nur ausgeliefert, wenn sie **beim Build** im Clone lagen. Volume-Medien allein reichen nicht.

**Lösung:** `app/media/[...path]/route.ts` + `lib/public-media-file.ts` — dynamisches Streaming aus dem gemounteten Ordner (Range-Requests für Video).

---

## 3. Ops (Server, nicht im Repo)

| Schritt | Ergebnis |
|---------|----------|
| `/data/schulnavigator/{media,dialog-audio,coach-audio}` | angelegt, uid 1001 |
| Coolify Application 5 | drei Bind-Mounts in `docker-compose.yaml` |
| Initial-rsync | vom MPZ-Rechner; `chown` danach |
| `docker exec -u nextjs … test -r /app/public/media` | OK |
| `/api/health` Prod | 200 |

**Hinweis rsync:** Ziel gehört uid 1001 → `--rsync-path="sudo rsync"` + `sudo chown -R 1001:1001` (in `fuer-entwickler.md`).

---

## 4. Offen nach diesem Schritt

1. **Coolify Prod:** Branch auf **`kunde/39-gs`** stellen (Commit `cf2d0d0`+) → Redeploy — **kein** Merge nach `main`  
2. **Prod-Smoke:** `/media/…`, `/api/dialog/…`, `/api/coach/welcome-hub`, `/stations-icons/…` (#228-Icons auf `kunde/39-gs`)  
3. **Issue #229 schließen** erst nach grünem Prod-Smoke  
4. **#230** Deploy-Automatisierung (`deploy-content.sh`)

---

## 5. Risiken

| Risiko | Schwere | Mitigation |
|--------|---------|------------|
| Prod läuft noch Commit `9938eb0` (alter `main`-Stand) | Mittel | Coolify → Branch `kunde/39-gs`, Redeploy |
| `/media/*` 404 ohne Media-Route | Hoch — behoben im Code | Route deployen |
| `welcome-hub.wav` nicht in Git (Bahn B) | Niedrig | Auf Volume rsync'd; lokal vor rsync aus LFS-Historie |
