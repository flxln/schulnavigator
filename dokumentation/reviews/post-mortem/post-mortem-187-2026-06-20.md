---
tags:
  - review
  - post-mortem
erstellt: 2026-06-20
issue: 187
---
# Post-Mortem 4 — Plan-Code-Abgleich nach Implementierung (Issue #187)

**Plan:** [MPZ Medium Replace #187](../../../.cursor/plans/mpz-medium-replace_#187.plan.md)

Der Code aus `#187` entspricht exakt dem gehärteten Plan. Alle spezifizierten Edge-Cases, Fehler-Mappings und IO-Vorsichtsmaßnahmen wurden vorbildlich umgesetzt.

---

## 1. Todos

| id | Befund |
|---|---|
| domain | ✅ Exakt implementiert: `replaceStationMediumFile(input, io)` nutzt bestehende Hilfsfunktionen. |
| exports | ✅ `uniqueDiskPath` und `tryDeleteMediaFile` wurden korrekt exportiert und verwendet. |
| api | ✅ POST-Route implementiert und mit Guard versehen. |
| tests-domain | ✅ `mpz-medium-replace.test.ts` testet alle spezifizierten Cases (In-place, Endungswechsel, Kollisionen). |
| tests-route | ✅ `file/route.test.ts` deckt Guard (ohne `message`), Fehler-Mapping und Happy-Path ab. |
| verify | ✅ Tests existieren und kompilieren. |

---

## 2. Änderungslog-Einträge (Pre-Mortem-Fixes)

| Eintrag | Befund |
|---|---|
| ¹ `io`-Parameter ergänzt | ✅ `io = createMpzContentIo()` ist konsequent für FS-Isolation in Tests genutzt. |
| ² `INVALID_TYP` → `FIELD_NOT_ALLOWED` | ✅ Erwartungsgemäß umgesetzt für YouTube und Links. |
| ³ In-Place-Reihenfolge (persist-first) | ✅ Korrekt umgesetzt: `persistFile` in `.tmp`, dann `writeStations`, dann Rename-Dance. |
| ⁴ `videoSource: 'upload'` immer setzen | ✅ Kanonisierung bei Video ohne Source ist vorhanden. |
| ⁵ Disk-Kollision via `uniqueDiskPath` | ✅ `-2` Suffix bei geteilter Quelle und Kollision wird korrekt angewandt. Kein Fehler-Reject. |
| ⁶ Guard-Body-Lücke dokumentiert | ✅ In `route.test.ts` explizit validiert (`not.toHaveProperty('message')`). |
| ⁷ `uniqueDiskPath` / `tryDeleteMediaFile` exportieren | ✅ Korrekt aus ihren Ursprungsdateien exportiert und wiederverwendet. |

---

## 3. Entscheidungstabelle

Der Code spiegelt die Entscheidungen aus dem Plan vollständig wider:
- **Fehler-Mapping:** Alle Domänen-Fehler (wie `NOT_FOUND`, `FIELD_NOT_ALLOWED` und `MpzUploadError`) werden in der API korrekt auf die HTTP-Status-Codes 404, 422 und 500 gemappt.
- **Typ-Signaturen:** `ReplaceMediumFileInput` und `ReplaceMediumFileResult` entsprechen der Vorgabe.
- **Merge-/Write-Reihenfolge:** Das "persist-first"-Pattern mit nachgelagertem Löschen (`isMediaPathStillReferenced`) nach der JSON-Speicherung schützt sauber vor 404-Deadlinks.

---

## 4. Scope

- Keine unautorisierten oder stillen Refactorings.
- Die private Helper-Methodik (`extensionFromQuelle`, `normalizeUploadExtension`) fügt sich sauber ein.
- Das Modul beschränkt sich exakt auf die geforderte Funktionalität für das MPZ Medium Replace.
